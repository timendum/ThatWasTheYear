{
  "name": "that-was-the-year",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun build src/app.ts --outdir=dist --target=browser && bun run copy-assets",
    "copy-assets": "bun run scripts/copy-assets.ts",
    "serve": "bun run --hot scripts/server.ts",
    "check-songs": "bun run scripts/check-songs.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "module": "index.ts",
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}
