name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - name: Type check
        if: always()
        run: bun run tc
      - name: Lint
        if: always()
        run: bun run lint
      - name: Format check
        if: always()
        run: bun run fmt:check
      - name: Validate songs.json
        if: always()
        run: bun run validate-songs
      - name: Run tests
        if: always()
        run: bun test
